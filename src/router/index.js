import Vue from 'vue'
import Router from 'vue-router'
// import Taobao from '@/components/taobao'

Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/taobao',
      name: 'Taobao',
      // component: Taobao,
    },
  ],
})
